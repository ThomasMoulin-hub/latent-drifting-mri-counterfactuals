# @package _global_

defaults:
  - override /data: oasis
  - override /model: diffusion
  - override /logger: wandb
  - override /trainer: default

tags: ["oasis", "diffusion", "conditional"]

seed: 42

trainer:
  min_epochs: 1
  max_epochs: 100
  accelerator: auto

data:
  batch_size: 14
